[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "alembic upgrade head && gunicorn main:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
